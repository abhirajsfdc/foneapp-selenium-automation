[pytest]
testpaths = tests
addopts =
    -v
    --tb=short
    --html=reports/report.html
    --self-contained-html
log_cli = true
log_cli_level = INFO

markers =
    live: tests that use real credentials and may trigger OTP/SMS — run manually only
